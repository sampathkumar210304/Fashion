import React from 'react';
import '../styles/HeroBanner.css';

const HeroBanner = () => (
  <section className="hero-banner" id="hero">
    <h1>Trendy Fashion for Everyone</h1>
    <p>Explore the latest collections now!</p>
  </section>
);

export default HeroBanner;
